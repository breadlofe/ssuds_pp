{
    "version": "0.2.0",
    "configurations": [
    {
        "name": "Cmake build and launch",
        "type": "cppdbg",
        "request": "launch",
        "program": "${workspaceRoot}/bin/main",
        "cwd": "${workspaceRoot}",
        "preLaunchTask": "Build"
    }
    ]
}
